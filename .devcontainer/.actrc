# This file is used to configure ACT behaviour
# notably to exclude certain workflows and jobs from being run locally.
# See docs/dx-setup.md for more information.

# TODO: i'm not sure where this needs to be placed

# Exclude all deployment-related workflows
-W '!.github/workflows/*deploy*.yml'
-W '!.github/workflows/*prod*.yml'
-W '!.github/workflows/release*.yml'

# Exclude deployment-related events
--exclude-events "deployment,deployment_status,release,workflow_dispatch"

# Exclude specific jobs
--exclude "deploy-*"
--exclude "*-prod"
--exclude "publish-*"
